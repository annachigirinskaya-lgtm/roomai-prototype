'use client';
export default function ProductGrid({products}:{products:any[]}){async function click(p:any){try{await fetch('/api/affiliate/click',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({productId:p.id,store:p.store})})}catch{}window.open(p.affiliate_url||p.product_url,'_blank','noopener,noreferrer')}return <div className="grid md:grid-cols-3 gap-4">{products.map(p=><div className="card overflow-hidden" key={p.id}><img src={p.image_url} alt={p.title} className="w-full aspect-[4/3] object-cover"/><div className="p-5"><div className="text-xs uppercase tracking-wide text-stone-500">{p.store} · {p.category}</div><h3 className="font-semibold mt-2">{p.title}</h3><p className="text-sm text-stone-600 mt-2">{p.description}</p><div className="text-xl font-bold mt-4">${Number(p.price).toFixed(2)}</div><button onClick={()=>click(p)} className="btn-soft w-full mt-4">View product</button></div></div>)}</div>}
