import OpenAI, { toFile } from 'openai';
import { Product, ProjectInput } from '@/lib/types';
import { STYLE_GUIDANCE } from '@/lib/catalog';
export function designPrompt(p:ProjectInput,products:Product[]){const shopping=products.map(x=>`${x.category}: ${x.title} (${x.store}, $${x.price})`).join('; ');const signature=STYLE_GUIDANCE[p.style]||p.style;return `Create a photorealistic furnished redesign by editing the PROVIDED PHOTO of this ${p.room_type}; it must remain recognizably the exact same room and camera view. Preserve the architecture, room proportions, perspective, windows, doors, ceiling, flooring and fixed structures unless the client explicitly requests otherwise. Apply one unmistakable style only: ${p.style}. The required style signature is: ${signature}. Do not drift into generic beige contemporary design and do not mix in visual signatures from the other catalog styles. Palette: ${p.color_palette}${p.custom_colors?`, custom colors: ${p.custom_colors}`:''}. Target budget: $${p.budget}. Budget mode: ${p.budget_mode}. Keep these existing items visibly recognizable: ${p.keep_items||'none specified'}. Replace or add: ${p.replace_items||'furniture, lighting, textiles and decor as appropriate'}. Additional client notes: ${p.notes||'none'}. ${shopping?`Use this real-product plan as visual inspiration: ${shopping}.`:''} Add believable furniture, lighting, textiles, wall treatment and decor with correct scale, shadows and perspective. Do not return a mood board, collage, empty room, floor plan, text, labels or a different property. Make the result attainable, polished and suitable for a US home.`}
export const IMAGE_MODEL=process.env.OPENAI_IMAGE_MODEL||'gpt-image-1.5';
export const IMAGE_SIZE='1536x1024' as const;
export const IMAGE_QUALITY='medium' as const;
export async function generateFromRoom(source:Buffer,mime:string,p:ProjectInput,products:Product[]){if(!process.env.OPENAI_API_KEY) throw new Error('OPENAI_API_KEY missing');const client=new OpenAI({apiKey:process.env.OPENAI_API_KEY});const file=await toFile(source,'room',{type:mime});const res=await client.images.edit({model:IMAGE_MODEL,image:file,prompt:designPrompt(p,products),size:IMAGE_SIZE,quality:IMAGE_QUALITY});const b64=res.data?.[0]?.b64_json;if(!b64)throw new Error('No image returned');return Buffer.from(b64,'base64')}
